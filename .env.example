# AWS Configuration for S3 Data Fetching - Example Template
# Copy this file to .env and fill in your actual values
# OR run 'npm run setup-dev' for automatic configuration

# AWS SSO Configuration
# Run 'npm run setup-dev' to automatically configure these
AWS_PROFILE=default
AWS_REGION=us-east-2

# S3 Configuration
AWS_S3_BUCKET_NAME=blw-dataviz-data
AWS_S3_DATA_PREFIX=path/to/your/data/

# AWS SSO Session (if using named SSO session)
# AWS_SSO_SESSION=your-sso-session-name

# Alternative: Direct AWS Credentials (not recommended for production)
# AWS_ACCESS_KEY_ID=your-access-key-here
# AWS_SECRET_ACCESS_KEY=your-secret-key-here

# Optional: Custom endpoint for local testing
# AWS_ENDPOINT_URL=http://localhost:4566

# Script Configuration
DATA_DOWNLOAD_PATH=./downloads
CACHE_DURATION_HOURS=24

# Debug and Logging
LOG_LEVEL=info
AWS_CLI_AUTO_PROMPT=off

# Development Notes:
# - Run 'npm run setup-dev' for first-time environment setup
# - The setup process will help configure AWS SSO authentication
# - Data files are downloaded to public/ directory (gitignored)
# - Re-run 'npm run fetch-data' when you need updated data from S3
AWS_CLI_AUTO_PROMPT=off